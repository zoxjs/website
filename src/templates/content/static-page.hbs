<div class="container">
    <div class="row">

        <div class="col-lg-2"></div>

        <div class="article col-lg-8 pad-lg">
            <h1>{{title}}</h1>
            {{#if date}}<p class="date">{{formatDate date 'd. mmm yyyy.'}}</p>{{/if}}
            <div class="body">{{{body}}}</div>
        </div>

        <div class="col-lg-2"></div>

    </div>
</div>